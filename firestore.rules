rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All authenticated users (employees + admin) can read/write
    // This is appropriate for an internal company tool
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
